{
  "name": "foundry",
  "version": "0.1.0",
  "private": true,
  "description": "An agent process for building software meant to last. Plain-markdown commands for Cursor, Claude Code, and Codex.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "shapes": "node scripts/generate-command-shapes.js",
    "test": "node --test",
    "check": "node --test && node scripts/generate-command-shapes.js --confirm && node scripts/check-links.js && node scripts/check-jargon.js"
  }
}
